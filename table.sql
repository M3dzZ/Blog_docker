            $sql = CREATE TABLE `authentification` (
                `id` int(11) NOT NULL,
                `username` varchar(255) NOT NULL,
                `password` varchar(255) NOT NULL
            );

                $sql = CREATE TABLE `posts` (
                `id_comment` int(11) NOT NULL,
                `comments` varchar(255) NOT NULL,
            );
